import React from 'react';
import { Icon } from '../atoms/Icon';

export function Header() {
  return (
    <header className="bg-olive-900 text-white shadow-soft relative overflow-hidden">
      {/* Elemento decorativo de fundo (opcional, para profundidade) */}
      <div className="absolute top-0 right-0 -mr-20 -mt-20 w-64 h-64 bg-sage-500 rounded-full blur-3xl opacity-10 pointer-events-none"></div>

      <div className="max-w-[1200px] mx-auto px-4 md:px-8 py-10 md:py-14 relative z-10">
        <div className="flex flex-col md:flex-row md:items-start md:justify-between gap-6">
          <div className="space-y-3 max-w-2xl">
            <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-olive-800/50 border border-olive-700 text-sage-300 text-xs font-medium tracking-wide uppercase">
              <Icon name="calculator" size="sm" />
              <span>Planejamento Financeiro</span>
            </div>
            
            <h1 className="text-3xl md:text-4xl lg:text-5xl font-bold tracking-tight text-white leading-tight">
              Calculadora de <span className="text-sage-400">Aquisição de Veículo</span>
            </h1>
            
            <p className="text-olive-200 text-lg leading-relaxed">
              Compare os custos reais entre comprar à vista, financiar ou assinar um carro. 
              Tome decisões baseadas em dados, não em palpites.
            </p>
          </div>

          {/* Badge ou Info extra (Desktop only) */}
          <div className="hidden md:block bg-olive-800/30 backdrop-blur-sm p-4 rounded-xl border border-olive-700/50 max-w-xs">
            <div className="flex items-start gap-3">
              <div className="p-2 bg-sage-500/20 rounded-lg text-sage-300">
                <Icon name="info" size="md" />
              </div>
              <div>
                <p className="text-sm font-medium text-sage-200">Como funciona?</p>
                <p className="text-xs text-olive-300 mt-1">
                  Consideramos depreciação, custo de oportunidade (CDI), seguro, IPVA e manutenções para calcular o Custo Efetivo Total.
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </header>
  );
}