/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'Roboto', 'sans-serif'], // Assegure-se de importar a fonte no index.html ou CSS
        mono: ['"Fira Code"', 'ui-monospace', 'SFMono-Regular', 'monospace'], // Para números financeiros
      },
      colors: {
        // Paleta Soft Sage (Verde Sálvia) - Primária
        sage: {
          50: '#F4F7F3',
          100: '#E6EFE4',
          200: '#D2E0CE',
          300: '#BDD0B8',
          400: '#ACC8A2', // Base
          500: '#8EB382',
          600: '#729866',
          700: '#58764E',
          800: '#405639',
          900: '#2B3926',
        },
        // Paleta Deep Olive (Oliva Profundo) - Texto/Estrutura
        olive: {
          50: '#F2F3F2',
          100: '#E0E3DF',
          200: '#C2C8C0',
          300: '#A3ADA0',
          400: '#859281',
          500: '#687764',
          600: '#4E5A4B',
          700: '#353E33',
          800: '#1F251E',
          900: '#1A2517', // Base
        },
        // Cores dos Cenários (Para Gráficos e UI)
        scenario: {
          cash: '#58764E',      // Sage Escuro (Família da marca)
          financed: '#C89F65',  // Warm Sand/Tan (Terra Quente)
          rental: '#64748B',    // Slate Blue/Grey (Neutro Frio)
        },
        // Cores Semânticas (Dessaturadas para manter o profissionalismo)
        status: {
          success: '#4F8A58',   // Verde floresta suave
          error: '#D65A5A',     // Vermelho terracota
          warning: '#E3A048',   // Âmbar suave
          info: '#5B8C9B',      // Azul petróleo suave
        }
      },
      boxShadow: {
        'soft': '0 4px 20px -2px rgba(26, 37, 23, 0.08)', // Sombra customizada suave
        'glow': '0 0 0 4px rgba(172, 200, 162, 0.3)',     // Anel de foco Sage
      },
      animation: {
        'slide-down': 'slideDown 300ms cubic-bezier(0.87, 0, 0.13, 1)',
        'shimmer': 'shimmer 2s linear infinite',
      },
      keyframes: {
        slideDown: {
          from: { height: '0', opacity: '0' },
          to: { height: 'var(--radix-accordion-content-height)', opacity: '1' },
        },
        shimmer: {
          from: { backgroundPosition: '0 0' },
          to: { backgroundPosition: '-200% 0' },
        },
      },
    },
  },
  plugins: [],
}